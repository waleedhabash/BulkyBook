@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row p-4">
        <div class="col-6">
            <h2 class="text-primary">Companys List</h2>
            
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Upsert" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> &nbsp; New</a>
        </div>
    </div>



    <table id="myTable" class="table table-bordered table-hover table-striped" style="width:100%">
    <thead>
        <tr>
            <th scope="col">
                Name
            </th>
            <th scope="col">
                    Street Address
            </th>
                <th scope="col">
                    City
                </th>
                <th scope="col">
                    State
                </th>
                <th scope="col">
                    Postal Code
                </th>
                <th scope="col">
                    Phone Number
                </th>
               
            <th scope="col">

            </th>
        </tr>
    </thead>
    @* <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td  scope="row" >
                    @item.Id
                </td>
                <td  scope="row">
                    @item.Name
                </td>
                    <td scope="row">
                        @item.ISBN
                    </td>
                    <td scope="row">
                        @item.Price
                    </td>
                    <td  scope="row">
                        @item.Author
                    </td>
                    <td  scope="row">
                        @item.CategoryId
                    </td>
                <td width="60%" scope="row">
                   <div class="w-75 btn-group" role="group">
                            <a class="btn btn-info mx-2" asp-controller="Product" asp-action="Edit" asp-route-Id="@item.Id"><i class="bi bi-pencil-square"></i> Edit</a>
                            <a class="btn btn-danger mx-2" asp-controller="Product" asp-action="Delete" asp-route-Id="@item.Id"><i class="bi bi-trash"></i> Delete</a>

                   </div>

                </td>
            </tr>
        }
    </tbody> *@
</table>
</div>
@section Scripts{
    <script src="~/js/company.js"></script>
}